"""Google Drive API test calls for credential validation (not implemented).

`validate` checks the Google Drive secret's shape but does not yet call the
Google APIs to confirm the credentials work.
"""
